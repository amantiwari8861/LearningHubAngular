import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';


@Component({
  selector: 'app-oneway',
  imports: [FormsModule],
  templateUrl: './oneway.html',
  styleUrl: './oneway.css'
})
export class Oneway {
  // isHighlighted: boolean = false;
  // hasError: boolean = false;

  // dynamicClassesObject = {
  //   'border-blue': true,
  //   'text-center': false,
  //   'large-font': true
  // };

  // dynamicClassesArray: string[] = ['border-blue', 'large-font'];

  // toggleHighlight() {
  //   this.isHighlighted = !this.isHighlighted;
  // }

  // toggleError() {
  //   this.hasError = !this.hasError;
  // }

  // // style-binding 

  // textColor: string = 'blue';
  // fontSizePx: number = 16;

  // dynamicStylesObject = {
  //   'background-color': 'lightgreen',
  //   'border': '2px dashed purple',
  //   'padding': '10px',
  //   'text-align': 'center'
  // };

  // changeColor() {
  //   this.textColor = this.textColor === 'blue' ? 'green' : 'blue';
  // }

  // increaseFontSize() {
  //   this.fontSizePx += 2;
  // }

  // name!: string;
  // name?: string;


}
